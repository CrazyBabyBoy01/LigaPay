# ⚙️ Django
SECRET_KEY=

# 📦 Postgres
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# 📧 Email
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=

# 🔄 Redis / Celery
REDIS_HOST=localhost
REDIS_PORT=6379

# 🔧 Docker flag (0 = local, 1 = Docker)
RUN_FROM_DOCKER=0
